{
  "name": "lunchtable-ttg",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/web && bun run build",
    "convex:codegen": "bun run setup:esbuild && npx convex codegen",
    "dev": "concurrently -n convex,web -c blue,magenta \"bun run dev:convex\" \"bun run dev:web\"",
    "dev:convex": "convex dev",
    "dev:web": "cd apps/web && bun run dev",
    "models:list": "bun run scripts/fetch-provider-models.ts",
    "format": "bunx @biomejs/biome format --write .",
    "format:check": "bunx @biomejs/biome format .",
    "lint": "bunx @biomejs/biome lint .",
    "lint:fix": "bunx @biomejs/biome lint --write .",
    "bench:studio-api": "bun scripts/bench-studio-api.ts",
    "test": "(cd apps/web && bunx vitest --config vitest.config.ts) && (cd convex && bunx vitest --config vitest.config.ts)",
    "test:once": "(cd apps/web && bunx vitest run --config vitest.config.ts) && (cd convex && bunx vitest run --config vitest.config.ts)",
    "type-check": "bunx tsc --project convex/tsconfig.json && bunx tsc --project apps/web/tsconfig.json && bunx tsc --project packages/plugin-ttg/tsconfig.json",
    "usage:fetch": "bun run scripts/fetch-provider-usage.ts",
    "setup:esbuild": "bun install --cpu '*'",
    "hygiene": "bun run format && bun run lint:fix && bun run type-check",
    "hygiene:check": "bun run format:check && bun run lint && bun run type-check"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^1.2.12",
    "@ai-sdk/openai": "^1.3.22",
    "@sentry/node": "^10.38.0",
    "ai": "^4.3.19",
    "convex": "^1.31.6",
    "posthog-node": "^5.24.15"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "typescript": "^5.8.0",
    "vitest": "^4.0.18"
  },
  "optionalDependencies": {
    "@esbuild/darwin-arm64": "0.27.0",
    "@esbuild/darwin-x64": "0.27.0"
  },
  "module": "index.ts"
}
